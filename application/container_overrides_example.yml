# This file contains example configuration and can be copied to
# container_overrides_example.yml to override the contents of container.yml.
parameters:
  log.file: '/var/log/tam1.log'
  solr.phantom:
    endpoint:
      localhost:
        host: 192.168.50.5
        port: 8080
        path: /solr/phantomcore
        timeout: 10
  cpu_count: 4
  quail.worker_count: 30
